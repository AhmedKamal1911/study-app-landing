import { Box, Container } from "@mui/material";

const SimilarCoursesSection = ({ courseId }) => {
  // TODO: fetch similar courses to the courseId
  return (
    <Box>
      <Container maxWidth="xl">test</Container>
    </Box>
  );
};

export default SimilarCoursesSection;
